import helper from 'dummy/utils/helper';
import { module, test } from 'qunit';

module('Unit | Utility | helper', function () {

	// TODO: Replace this with your real tests.
	test('it works', function (assert) {
		let result = helper();
		assert.ok(result);
	});
});
