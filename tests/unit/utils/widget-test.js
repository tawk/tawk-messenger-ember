import widget from 'dummy/utils/widget';
import { module, test } from 'qunit';

module('Unit | Utility | widget', function () {

	// TODO: Replace this with your real tests.
	test('it works', function (assert) {
		let result = widget();
		assert.ok(result);
	});
});
